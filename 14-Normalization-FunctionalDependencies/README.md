## **NORMALIZATION** & <br> **FUNCTIONAL DEPENDENCY** 🎲

#### Normalizaton is a basically technique in database which is used for decompose the large database table into smaller database table and provide relationship between them internally called as normalization.
          